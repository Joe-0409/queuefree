从现在开始，前端 Batch 16 已完成 mobile 写操作第一轮 generated bridge 接线。

已接通：
- createOrder
- createPaymentIntent
- checkInQueueGuard

本轮没有新增任何冻结项：
- 不新增 route
- 不新增 env var
- 不新增 request/response field
- 不改 packages/shared
- 不改 packages/api-client 生成边界

当前剩余依赖：
- backend 提供 SKU readonly 来源
- backend 提供 address readonly 来源
- backend 提供 payment success return / order status readonly contract（至少一项）

服务器侧只需要保证现有 API base URL 与 provider checkout URL 可达，不需要新增前端环境变量。
