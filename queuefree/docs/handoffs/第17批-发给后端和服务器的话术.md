从现在开始，前端 Batch 17 已完成移动端"续付 / 状态检查 / 队列可见性本地回流"闭环，但没有新增任何 registry 项。

请后端/服务器继续按以下优先级执行：
1. queuefree_prd_v1_2
2. docs/contracts/queuefree-collaboration-contract-v1.2.md
3. docs/registry/registry-baseline-v1.2.md
4. packages/shared
5. packages/api-client

当前前端还需要的只读真相源：
- SKU readonly source
- address-book readonly source
- order-status readonly source
- payment provider return strategy

注意：
- 不新增 route
- 不新增 env var
- 不手写 DTO 给前端
- 继续通过 OpenAPI → generate:api-client → frontend consume 的顺序推进