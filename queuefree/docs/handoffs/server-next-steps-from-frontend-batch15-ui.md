# 给服务器的衔接说明（Batch 15 UI）

本批是纯前端视觉补丁。

## 没改的东西
- 域名不变
- 环境变量不变
- 路由不变
- API path 不变

## 服务器当前无需改动
- 不需要新增反向代理规则
- 不需要新增静态资源域名
- 不需要新增 CI 步骤

## 可选后续
如果后续要把 Storybook 挂到预览环境，再单独登记 preview 子路径或独立子域名。
本批不会先行占用任何新域名或路径。
